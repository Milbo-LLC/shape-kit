{
  "name": "@shape-kit/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc -w --preserveWatchOutput",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "vitest"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.0",
    "drizzle-orm": "^0.30.9"
  },
  "devDependencies": {
    "@shape-kit/config": "workspace:*"
  }
}
